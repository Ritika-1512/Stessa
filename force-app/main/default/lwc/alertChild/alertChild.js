import { LightningElement, api } from 'lwc';

export default class AlertChild extends LightningElement {
    /* @api message
     @api cardHeading
     @api number
     @api isValid*/
    @api carouselDetails
}